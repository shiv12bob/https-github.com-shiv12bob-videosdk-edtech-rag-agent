# Examples

Place sample queries here. Example:

1. Query inside docs:
Q: "How long are recordings stored?"
Expected: Agent answers using doc context.

2. Query outside docs:
Q: "What's the capital of Italy?"
Expected: Agent falls back to LLM and answers "Rome".
